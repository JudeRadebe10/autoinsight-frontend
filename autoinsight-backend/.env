# ── Application ───────────────────────────────
APP_NAME="AutoInsight ZA"
PORT=8765

# ── Database (PostgreSQL) ──────────────────────
# For local dev, we default to a standard local postgres. Change in production.
DATABASE_URL=sqlite:///autoinsight_db.sqlite

# ── JWT ────────────────────────────────────────
JWT_SECRET_KEY=9a2b5c7e8d1f0a2b3c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a6b
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# ── CORS / Frontend ────────────────────────────
ALLOWED_ORIGINS=https://symphonious-palmier-e70423.netlify.app,http://localhost:3000,http://localhost:8765,http://127.0.0.1:8765

# ── Seeded default users ───────────────────────
SEED_SUPER_ADMIN_EMAIL=superadmin@example.com
SEED_SUPER_ADMIN_PASSWORD=SuperAdmin123!
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_PASSWORD=Admin123!
SEED_CLIENT_EMAIL=client@example.com
SEED_CLIENT_PASSWORD=Client123!
